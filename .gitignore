node_modules/
dist/
.chrome-debug-profile/
*.js.map
dist.crx
dist.pem
dom-after-images.html
flow-dom.html
flow_page_with_all_media.html
page_with_videos.html
whenimageisaddtotheprompt.html
# Secrets — never commit .env files
.env
.env.*
*.env
.vercel

# Local-only skill wiring: .agents/skills is a junction to .agent/skills.
# Not a portable artifact, so keep it out of git.
.agents/
__pycache__/

# graphify knowledge-graph output — regenerable from source, ~8 MB
graphify-out/

# Generated by studio-extension/scripts/publish-templates.js. Derivable from
# src/studio/templates/index.ts, and 150KB of churn on every publish.
dist-templates/

# Local development database. Holds real user rows once the app has been run,
# so it must never reach the repo — and it showed as untracked on every
# status, which trains you to ignore the list.
db.sqlite3
*.sqlite3
