fastapi==0.115.0
uvicorn[standard]==0.30.0
pydantic-settings==2.5.0
python-multipart==0.0.9
# 2.x is required for Gemini 3: thinking_level, and VideoMetadata.fps for
# sampling frames sparsely. 1.x has neither and fails at call time.
google-genai>=2.19.0
google-cloud-storage>=2.14.0
google-auth>=2.30.0
# supabase was removed, not loosened: nothing imports it. The only trace was
# three unused settings in config.py, and a comment saying the in-memory job
# store should "replace with Supabase in production" — planned, never built.
#
# It had to go because it pinned httpx<0.28 while google-genai needs >=0.28.1,
# which is irreconcilable and failed the build. Keeping a dependency nothing
# uses, at the cost of the one that does the work, is the wrong trade.
# supabase==2.9.0
stripe==10.0.0
PyJWT>=2.8.0
yt-dlp>=2024.1.0
httpx>=0.27.0
aiofiles>=24.0.0
json-repair>=0.30.0

# Face detection for vertical reframing, plus the video decoding to feed it.
# headless because this server has no display: the GUI build pulls libGL and
# friends, which a slim image does not have and does not want.
#
# It replaces a model call per clip. Measured on the same footage: the
# whole-video reading answered where the speaker was for 0 of 8 scenes, a
# dedicated model ask was right but cost 18.6s per clip, and this agrees with
# that ask to 0.009 of frame width at 14.8ms per frame.
opencv-python-headless>=4.9.0
